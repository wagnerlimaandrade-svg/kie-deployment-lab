apiVersion: v1
kind: Service
metadata:
  name: {{ include "kie-runtime.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kie-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kie-runtime.selectorLabels" . | nindent 4 }}
